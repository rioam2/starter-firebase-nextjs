import styled from 'styled-components';

export const AppViewStyles = {
    wrapper: styled.div`
        position: relative;
        display: block;
        width: 100%;
        height: 100%;
        background-color: #f1f1f1;
    `
};
